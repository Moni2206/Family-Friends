"use client";

import { CiStar } from "react-icons/ci";

const Favorit = () => {
  return <CiStar size={30} className="absolute top-3 right-3 text-white bg-white/40 rounded-full p-1 z-10 cursor-pointer" />;
};

export default Favorit;

// "use client";

// import { CiStar } from "react-icons/ci";

// const Favorit = ({ onClick }) => {
//   return <CiStar size={30} onClick={onClick} className="absolute right-3 text-white bg-white/40 rounded-full p-1 z-10 cursor-pointer" />;
// };

// export default Favorit;
